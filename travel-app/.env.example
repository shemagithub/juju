# Copy to .env for local overrides.
# Leave REACT_APP_API_URL empty in development to use package.json proxy → http://localhost:4000

GENERATE_SOURCEMAP=false

# Production API (set in .env.production for builds)
# REACT_APP_API_URL=https://backend.rwandaquesttours.com
