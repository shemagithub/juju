# Copy to .env for local overrides.
# Default production API is already set in .env / .env.production.

GENERATE_SOURCEMAP=false
REACT_APP_API_URL=https://backend.rwandaquesttours.com

# Local API instead of the live backend:
# REACT_APP_API_URL=http://localhost:4000
